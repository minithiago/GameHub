import 'package:flutter/material.dart';

class GameHubLogo extends StatelessWidget {
  const GameHubLogo({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      "assets/images/gamehubLogo.png",
    );
  }
}

class GameHub extends StatelessWidget {
  const GameHub({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      "assets/images/gamehub.png",
    );
  }
}
